name: CSS Figma Export

on:
  repository_dispatch:
    types: [css-export]

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Display payload
        run: echo "Received CSS export event from Figma!"
